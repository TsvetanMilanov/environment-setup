- name: Create programs dir
  file:
    path: "{{ custom_programs_dir }}"
    state: directory

- include: google-chrome.yml
  tags: google-chrome

- include: wine.yml
  tags: wine

- include: keepass.yml
  tags: keepass
